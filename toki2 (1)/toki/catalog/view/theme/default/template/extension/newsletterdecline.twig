{{ header }} 
<div class="container">
  <div class="row">
  <div class="col-sm-12 text-center">
    {% if (newsletter_declinesuccessmsg_text is not empty) %} 
        <h2>{{ newsletter_declinesuccessmsg_text }}</h2>
    {% endif %} 
    </div>
  </div>
</div>

{{ footer }}